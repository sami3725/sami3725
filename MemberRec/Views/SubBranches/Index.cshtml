@model IEnumerable<MemberRec.Models.SubBranch>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubBranchName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubBranchName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubBranchId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubBranchId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubBranchId })
        </td>
    </tr>
}

</table>
